# fish-git-branch-prune

```
fisher install sters/fish-git-branch-prune
```

```
>>> git-branch-prune
Set your remote main branch (default: origin/master):
Remove these branches:aaa bbb ccc

Delete OK? Y or N (default: n):y

Deleted branch aaa (was 73368ef).
Deleted branch bbb (was 22df7aa).
Deleted branch ccc (was 4952bef).
```
